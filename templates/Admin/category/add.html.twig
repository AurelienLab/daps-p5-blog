{% extends 'Admin/base.html.twig' %}

{% block content %}
    <h2 class="text-base font-semibold leading-7 text-gray-900">Ajouter une catégorie</h2>
    <p class="mt-1 max-w-2xl text-sm leading-6 text-gray-600">Remplissez ici les informations pour ajouter
        une catégorie d'article qui sera ensuite disponible dans la selection lors de l'ajout / edition.</p>

    <form method="POST" action="{{ route('admin.category.add.post') }}">
        {% include 'Admin/category/_form.html.twig' %}
    </form>
{% endblock %}
