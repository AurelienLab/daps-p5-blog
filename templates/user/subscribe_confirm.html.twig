{% extends 'base-login.html.twig' %}

{% block content %}
    <main>
        <!-- SUBSCRIPTION FORM -->
        <section class="subscription">
            <div class="subscription__container">
                <h2 class="subscription__heading">Inscription</h2>
                <div class="subscription__content">
                    <h3>Il reste une dernière étape ! 📨</h3>
                    <p>Votre inscription n'est pas tout à fait terminée, il faut valider votre adresse email !</p>
                    <p>Un mail vous a été adressé à l'adresse indiquée juste avant, ouvrez le vite pour profiter des
                        fonctionnalités du blog !</p>
                    <a href="{{ route('homepage.index') }}" class="btn btn--outline--primary">Retour au site</a>
                </div>
            </div>
        </section>
    </main>
{% endblock %}
